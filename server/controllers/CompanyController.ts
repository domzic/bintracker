import { Request, Response, NextFunction } from 'express';

export default class CompanyController {

    static createCompany(req: Request, res: Response, next: NextFunction) {
        throw new Error("Method not implemented.");
    }
    
}